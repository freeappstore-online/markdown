{
  "name": "freemarkdownapp",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm --filter @markdown/web dev",
    "build": "pnpm --filter @markdown/web build",
    "preview": "pnpm --filter @markdown/web preview",
    "typecheck": "pnpm --filter @markdown/web exec tsc -b",
    "test": "pnpm --filter @markdown/web test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/freeappstore-online/markdown.git"
  }
}
